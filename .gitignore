# === Python ===
.venv/
venv/
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.mypy_cache/
.pytest_cache/
.ruff_cache/
htmlcov/
.coverage
coverage.xml

# === Node.js ===
node_modules/
# Sem a barra também: com ela o padrão casa só diretório, e um symlink chamado
# `node_modules` — que se cria à mão para reaproveitar dependências entre
# worktrees — passava direto por um `git add -A`. Aconteceu.
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm

# === Environment & Secrets ===
.env
.env.*
!.env.example
*.pem
*.key
credentials.json

# === IDE ===
.vscode/settings.json
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# === Claude Flow Runtime (data local, não versionável) ===
.swarm/
.claude-flow/data/
.claude-flow/logs/
.claude-flow/sessions/
.claude-flow/metrics/
.claude-flow/learning/
.claude-flow/security/
.claude/memory.db

# === Build & Output ===
dist/
out/
*.tgz

# === Artefatos de migracao de PC (nao versionar) ===
RESTORE.md
_venv_requirements/
.vercel
.env*
# Reafirmado depois de `.env*`: em gitignore a ultima regra que casa vence, e o
# `!.env.example` declarado mais acima estava sendo anulado por esta secao.
!.env.example

# === Relatorios de cobertura (gerados por `npm run test:coverage`) ===
coverage/
*.lcov

# === Event store e projecao (artefatos de runtime, nunca versionados) ===
# O log e a view materializada sao dados operacionais. O ADR-003 move o store para
# Postgres por tenant; ate la o JSONL local fica fora do versionamento.
.roadmap/activity.jsonl
.roadmap/roadmap.json
data/

# Material de referência licenciado (livros) - nao versionar
docs/referencias/raw/
docs/referencias/chunks/
docs/referencias/*.md
